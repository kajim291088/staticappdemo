apiVersion: v1
kind: Service
metadata:
  name: staticappdemo-service
spec:
  type: NodePort
  selector:
    app: staticappdemo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
